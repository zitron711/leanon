<!-- SELECTED CARD DISPLAY (DEFAULT) -->
<div class="card-select-wrapper">
  <div class="card-select-toggle">
    <!-- This value comes from the current/default Card XF -->
    ${currentCard.title}
  </div>

  <!-- DROPDOWN OF AVAILABLE CARDS FROM CARD XF -->
  <div class="card-select-dropdown">
    <div class="card-row" data-sly-list.card="${cardXF.cards}">
      <div class="card-thumb">
        <img src="${card.image}" alt="${card.title}" />
      </div>
      <div class="card-title">
        <h2>${card.title}</h2>
      </div>
    </div>
  </div>
</div>
