.calculator-table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0 20px; /* Adds 20px vertical gap between header and rows */
}

/* Optional: clean up the table look */
.calculator-table th,
.calculator-table td {
  padding: 12px 16px;
  text-align: left;
  vertical-align: top;
}

/* First column: reward-percent cell */
.reward-percent {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 8px; /* gap between title and description */
}

/* Reward title and description styling */
.reward-title {
  font-weight: 600;
  font-size: 16px;
  color: #222;
}

.reward-description {
  font-size: 14px;
  color: #666;
}

/* Input field styling */
.monthly-spend {
  width: 100%;
  padding: 8px 12px;
  font-size: 14px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

/* Cashback cell styling */
.cashback {
  font-weight: bold;
  font-size: 16px;
  color: #0a4ebf;
}
