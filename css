.selected-cards-dropdown.hidden {
  display: none;
}

.selected-cards-dropdown.active {
  display: block;
  position: absolute;
  background: white;
  border: 1px solid #ccc;
  width: 100%;
  max-height: 300px;
  overflow-y: auto;
  z-index: 1000;
  padding: 10px 0;
}

.selected-cards-toggle {
  cursor: pointer;
  padding: 10px 15px;
  background: #f0f0f0;
  border: 1px solid #ccc;
  user-select: none;
}

.selected-cards-dropdown-list {
  padding: 8px 15px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 10px;
}

.selected-cards-dropdown-list:hover {
  background-color: #e6f0ff;
}

.selected-cards-dropdown-title {
  font-weight: 600;
}

.tick-icon.selected {
  content: '✔';
  color: green;
  margin-left: auto;
}
