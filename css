@media (max-width: 768px) {
  .modal-overlay {
    width: calc(100% - 10px); /* Leave 10px space on left */
    left: 10px;
    right: 0;
    transform: translateX(100%);
    padding: 0;
    max-width: unset;
  }

  .modal-box {
    width: 100%;
    height: 100%;
    right: 0;
    border-radius: 0;
    display: flex;
    flex-direction: column;
  }

  .modal-overlay.active {
    transform: translateX(0);
  }

  .modal-body {
    padding: 20px; /* Add 20px space inside modal */
    flex: 1; /* Makes content fill modal if needed */
    margin-top: 40px;
    overflow-y: auto;
  }

  .modal-close {
    top: 12px;
    right: 12px;
  }

  .modal-backdrop {
    background: rgba(0, 0, 0, 0.5);
  }

  .selected-card,
  .change-card-dropdown-list,
  .donut-item {
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .selected-card-img {
    width: 100px;
    height: auto;
  }

  .change-card-dropdown {
    width: 95%;
    left: 2.5%;
  }

  .custom-dropdown-toggle,
  .custom-dropdown-list {
    font-size: 16px;
    width: 100%;
  }

  .monthly-spend {
    font-size: 16px;
    padding: 6px 6px 6px 20px;
  }

  .cashback,
  .cashback-total {
    font-size: 16px;
  }

  .donut-image-style-bold,
  .donut-image-style-unenrolled,
  .donut-image-style-platinum,
  .donut-image-style-platinumHonors {
    width: 100%;
    top: 0;
    border-top-width: 3px;
  }

  .donut-overlay {
    top: 0;
    text-align: center;
  }

  .reward-value,
  .reward-detail {
    font-size: 14px;
    padding: 8px 0;
  }

  .card-calculator-cta {
    flex-direction: column;
    align-items: center;
  }

  .calculator-link {
    padding-left: 0;
    padding-bottom: 32px;
  }

  .change-card-wrapper {
    padding: 0 16px;
  }
}
